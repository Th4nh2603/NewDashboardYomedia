# @yomedia/database

Reserved for shared database primitives if persistence is later extracted from `apps/api`.

Current database ownership remains in `apps/api/src/database`. Do not move database runtime code here unless there is a clear package-boundary reason and the migration is planned.
