# @yomedia/test-utils

Reserved for reusable deterministic fixtures, authorization helpers, and test builders.

Do not add fake tests or helpers that hide missing infrastructure. Add utilities here only when multiple packages can use them.
