# @yomedia/auth

Reserved for shared authentication and authorization contracts, policy types, and frontend-safe auth DTOs.

Backend authentication and authorization logic currently belongs in `apps/api`.
