# Graders

Deterministic fixture validation is implemented by `scripts/run-evals.mjs`.

Optional model-based graders can be added here after the project chooses an evaluation runner and required secrets. Do not make CI depend on external API keys for deterministic checks.
